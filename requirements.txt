streamlit>=1.28.0

# The analysis library, pinned to a tag. A change in flowfreq cannot reach this
# app until this line is bumped, which is the point of pinning rather than
# tracking main.
flowfreq @ git+https://github.com/pinhead001/flowfreq@v0.3.0

# Declared because streamlit_app.py imports all four directly. A direct import
# should not rely on a transitive dependency, even though flowfreq pulls them in.
numpy>=1.20.0
pandas>=1.3.0
matplotlib>=3.4.0
scipy>=1.7.0
